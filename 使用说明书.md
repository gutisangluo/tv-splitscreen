# 创维TV 投屏分屏系统 — 使用说明书

---

## 一、系统简介

本系统可以将一台创维电视（酷开系统/Android）变成**多分区显示墙**。电视屏幕可以同时显示多个独立内容（图片、视频、网页、文字等），每个分区互不干扰。通过手机或电脑无线控制。

### 适用场景

- 店铺/商场信息展示墙
- 公司数据大屏
- 家庭信息面板
- 活动海报轮播

---

## 二、安装

### 1. 电视端安装

**方式一：U盘安装**

1. 将 `TV-App.apk` 复制到U盘
2. U盘插入电视，找到APK文件并点击安装
3. 如果提示"禁止安装未知来源应用"，去 **设置 → 安全 → 允许安装未知来源应用** 打开开关

**方式二：ADB安装（需电脑+adb工具）**

```bash
adb install -t -r TV-App.apk
```

### 2. 手机遥控端安装

- 安装 `RemoteCtrl-App.apk`（安装方式同上）
- 这是 WebView 包装的遥控器界面，你也可以直接用手机浏览器打开 `control-web/index.html`

### 3. 桌面端使用

- 双击 `TV控制端.exe`（无黑窗口，直接打开浏览器）
- 或直接用浏览器打开 `control-web/index.html`

---

## 三、连接流程

```
电视 ── 启动App ── 显示IP地址
  │
  └── 手机/电脑打开控制端
       │
       ├─ 输入电视IP（如 192.168.1.100）
       ├─ 端口 9527
       └─ 点「连接」

电视显示「已连接 ✓」→ 连接成功
```

### 关键点

- 电视和手机/电脑必须在**同一局域网**（连同一个WiFi）
- 电视IP显示在电视屏幕中央（启动后显示5秒，遥控器按「菜单」键可重新显示）
- 端口固定 **9527**，不需要改

---

## 四、基本操作

### 选择布局

控制端顶部有布局按钮：**全屏 / 水平二分 / 垂直二分 / 主副2:1 / 田字格4 / 3+1 / 2×3 / 3×3 / 4+1 / 自定义**

1. 点击布局按钮 → 预览区显示分区效果
2. 点击底部「发送布局」→ 电视屏幕分区生效

### 设置分区内容

1. 点击预览区中的某个分区（选中后高亮边框）
2. 在下方「分区内容」区域选择内容类型
3. 根据类型操作：

| 类型 | 操作 |
|------|------|
| 图片 | 选择图片 → 选显示模式 → 点「上传并应用」 |
| 视频 | 选择视频 → 勾选循环/静音 → 点「上传并应用」 |
| 网页 | 输入URL或粘贴HTML → 点「应用到分区」 |
| 文字 | 输入文字 → 调字号/颜色/对齐 → 点「应用到分区」 |
| 幻灯片 | 多选图片 → 设切换间隔 → 点「上传并应用」 |

### 使用全局工具

在分区内容下方有「**全局工具**」区域：

#### 📜 滚动文字（LED广告屏）

```
输入文字 → 选速度/位置(顶部/底部)/方向(向左/向右) → 点「发送滚动文字」
```

- 白色等宽字体 + **绿色发光**效果，像商场门口的LED屏
- 所有文字会从右到左（或左到右）**完整滚动一遍**
- 速度适中，匀速稳定

#### 🕐 时钟

```
选时间格式/字号/颜色/位置(四角) → 点「显示时钟」
点「隐藏」→ 隐藏时钟
```

### 清空分区

选中分区 → 点「清空此区」

### 刷新状态

点「刷新状态」查看电视当前各分区的内容类型

---

## 五、模拟器测试（开发者）

如果没电视，用 **雷电模拟器（LDPlayer）** 测试：

```bash
# 1. 启动模拟器

# 2. 装APK（用模拟器自带的adb）
F:\LDPlayer9\adb.exe -s emulator-5554 install -t -r TV-App.apk

# 3. 端口转发（把模拟器端口映射到电脑）
F:\LDPlayer9\adb.exe -s emulator-5554 forward tcp:9527 tcp:9527
F:\LDPlayer9\adb.exe -s emulator-5554 forward tcp:9528 tcp:9528

# 4. 启动电视端
F:\LDPlayer9\adb.exe shell monkey -p com.splitscreen.tv -c android.intent.category.LAUNCHER 1

# 5. 电脑浏览器打开 control-web/index.html
#    连接地址填 127.0.0.1:9527
```

---

## 六、常见问题

### Q: 控制端连接不上电视

1. 确认电视和手机在**同一个WiFi**下
2. 电视端是否已启动？电视上应该显示IP地址
3. 检查电视IP是否输入正确
4. 检查端口是否为 **9527**
5. 如果使用模拟器，确认 `adb forward` 已设置

### Q: 换了布局但电视画面没变化

- 选了布局后要点底部的「**发送布局**」按钮
- 只点布局按钮不会发送到电视

### Q: 滚动文字只显示前几个字

- 已修复。现在所有文字会完整滚动一遍
- 如还有问题，确认电视端已更新到最新版APK

### Q: 视频不能播放

- ⚠️ **已知问题**，正在修复中
- 已经换了系统 MediaPlayer，在部分电视上可能可以播放
- 图片、文字、网页、幻灯片均正常

### Q: 电视端显示「0.0.0.0」

- 网络接口获取IP失败
- 重启电视端App试一下
- 如果持续出现，检查电视的WiFi连接是否正常

### Q: 桌面端EXE双击没反应

- `TV控制端.exe` 是 **GUI 模式**（无CMD窗口）
- 双击后会自动打开默认浏览器
- 如果浏览器没自动弹出，手动打开 `http://127.0.0.1:随机端口`

---

## 七、端口说明

| 端口 | 用途 | 说明 |
|------|------|------|
| 9527 | WebSocket 控制 | 手机/电脑通过此端口发送指令 |
| 9528 | HTTP 文件上传 | 手机/电脑上传图片、视频到此端口 |

两个端口都在电视端监听，手机/电脑通过电视IP连接。

---

## 八、文件说明

最新版文件在 `E:\桌面\Hermes jb\脚本\tv-splitscreen\`：

| 文件 | 大小 | 用途 |
|------|------|------|
| `TV-App.apk` | ~6MB | 电视端安装包 |
| `RemoteCtrl-App.apk` | ~3MB | 手机遥控端安装包 |
| `TV控制端.exe` | ~5MB | 桌面控制端（双击开浏览器） |

---

## 九、技术参数

- **最低支持：** Android 7.0 (API 24)
- **推荐：** Android 9+ (API 28)
- **无需 root**
- **控制端：** 任意现代浏览器（Chrome / Edge / Safari）
- **通信：** 局域网 Wi-Fi
